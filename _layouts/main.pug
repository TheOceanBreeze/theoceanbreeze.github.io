doctype html
html(lang="en")
    include ../_includes/head
    body
        include ../_includes/nav
        if hero
            .header 
                h1 #{hero.title}
                h4 #{hero.subtitle}
        else
            .header
                h1 #{title}
        
        .content !{content}